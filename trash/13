Mecanismos de Búsqueda
========================
**By.className()**: devuelve el mecanismo de búsqueda basado en el atributo  HTML "class".

**By.name()**: devuelve el mecanismo de búsqueda basado en el atributo  HTML "name".

**By.tagName()**: devuelve el mecanismo de búsqueda basado en el nombre de la etiqueta HTML.

**By.id()**:  devuelve el mecanismo de búsqueda basado en el atributo  HTML "id".

**By.linkText()**:  devuelve el mecanismo de búsqueda basado en el texto contenido por los elementos con atributo "href". Debe cohincidir el texto "linkText" en su totalidad con
                                el texto del atributo.
                                
**By.partialLinkText()**: devuelve el mecanismo de búsqueda basado en el texto contenido por los elementos con atributo "href". Se tiene que encontrar la 
                                        subcadena "partialLinkText" dentro del texto del atributo.
                                        
**By.xpath()**: devuelve el mecanismo de búsqueda basado en la expresión xpath "xpathExpression".

**By.cssSelector()**:devuelve el mecanismo de búsqueda basado en el selector css "selector".

**getAttribute()**: se obtiene el valor del atributo "nameAttribute" de un elemento web determinado. El metodo devuelve un string por el contrario null.
