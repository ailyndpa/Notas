Clase select
========================

**Select**: Permite seleccionar de un conjunto de valores asociados al elemento que se pasa por parametro. 
        **Ejemplo**: Select sel= new Select(WebElement);